import React from 'react';
import HeaderPage from '../../components/HeaderPage';
import './DeckBuilder.scss';

const DeckBuilder = ({ name = '' }) => {
    return <HeaderPage>Deck Builder</HeaderPage>;
};

export default DeckBuilder;
